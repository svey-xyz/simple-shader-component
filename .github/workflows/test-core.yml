name: Core Tests

on:
  push:
    branches:
      - main
      - production
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  core:
    uses: ./.github/workflows/test-suite.yml
    with:
      suite: core
