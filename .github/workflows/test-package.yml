name: Package Tests

on:
  push:
    branches:
      - main
      - production
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package:
    uses: ./.github/workflows/test-suite.yml
    with:
      suite: package
